---
import { Background } from './Background.tsx'
import { Foreground } from './Foreground.tsx'
import { ExchangeColor } from './ExchangeColor.tsx'
---

<section
    class="bg-white border border-gray-200 shadow-md rounded-lg gap-8 py-8 px-12 flex flex-wrap items-center justify-start"
>
    <div class="flex flex-col gap-8 flex-wrap">
        <Background client:load />
        <Foreground client:load />
    </div>
    <ExchangeColor client:load />
</section>
